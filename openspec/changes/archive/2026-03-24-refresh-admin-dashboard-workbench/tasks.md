## 1. Information Architecture

- [x] 1.1 重构 `UI/coursearrange/src/views/dashboard/DashboardView.vue` 的首页结构，将页面改为“主视觉横幅 + 指标区 + 状态区 + 阶段清单”。
- [x] 1.2 删除“权限管理联调入口”卡片及其相关逻辑，不再在首页重复提供 `/system/rbac` 的显式 CTA。

## 2. Visual Design

- [x] 2.1 在不新增依赖的前提下，延续学院档案馆式视觉方向，强化标题、编号、签条、标签和状态卡层次。
- [x] 2.2 调整首页文案和静态模块内容，使其围绕系统主链状态、排课域概览和当前阶段重点展开，而不是权限联调说明。

## 3. Responsive Validation

- [x] 3.1 补齐桌面端和移动端布局规则，确保主要模块在窄屏下可折叠为单列且不重叠。
- [x] 3.2 运行前端构建验证，确认工作台页面优化后工程仍可正常打包。
